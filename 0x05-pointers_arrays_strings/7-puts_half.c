#include "main.h"

/**
 * puts_half - Entry point
 * @str: get string
 * Return: if string is odd return return
 */

void puts_half(char *str)
{
	int length = 0;

	while (str[length] != '\0')
	{
		length++
	}
	if (length % 2 != 0)
	{
		
	}
	return (length);
}
